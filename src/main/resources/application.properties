# ==========================
# RAILWAY DEPLOYMENT CONFIG
# ==========================
# This configuration uses Railway's internal environment variables.
# It is automatically used when the application is deployed on Railway.
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================
# HIBERNATE/JPA CONFIG
# ==========================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==========================
# SERVER CONFIG
# ==========================
server.port=${PORT:8080}

# ==========================
# LOCAL DEVELOPMENT CONFIG (Commented out)
# ==========================
# To run locally, uncomment these lines and comment out the Railway Deployment Config above.
# spring.datasource.url=jdbc:mysql://switchback.proxy.rlwy.net:25221/railway
# spring.datasource.username=root
# spring.datasource.password=tbjznUnpkmvTsDQRlZGMYMNrKlaydTHo
